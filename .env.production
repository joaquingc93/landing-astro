##########################################################################
# PRODUCTION ENVIRONMENT VARIABLES
#
# IMPORTANT: No incluyas credenciales reales (usuario Gmail, app password, 
# correos internos) en el repositorio. Configura los valores reales sólo en:
#   - Netlify: Site Settings > Build & Deploy > Environment
#   - Desarrollo local: crea un archivo `.env.local` (gitignored)
#
# Las credenciales que existían antes en este archivo deben considerarse
# EXPUESTAS. Revoca/rota inmediatamente el App Password de Gmail previo
# desde la consola de seguridad y genera uno nuevo exclusivo para producción.
##########################################################################

# WordPress API Configuration
WORDPRESS_API_URL=http://dodgerblue-rail-758925.hostingersite.com/wp-json
WORDPRESS_CUSTOM_API_URL=http://dodgerblue-rail-758925.hostingersite.com/wp-json/renovalink/v1
WP_MEDIA_URL=http://dodgerblue-rail-758925.hostingersite.com/wp-content/uploads

# Media domain normalization (used by normalizeMediaUrl)
# LOCAL apunta a tu instancia local (dev); PROD al dominio público usado en deploy.
# Estrategias posibles: env-swap | passthrough | proxy (proxy no usado ahora).
PUBLIC_WP_LOCAL_MEDIA_BASE=http://renovalinksite.local
PUBLIC_WP_PROD_MEDIA_BASE=http://dodgerblue-rail-758925.hostingersite.com
PUBLIC_WP_MEDIA_STRATEGY=env-swap

# API Cache Settings (segundos)
API_CACHE_TTL=300

# Node Environment (no colocar otros secretos aquí)
NODE_ENV=production

# Email Configuration (PLACEHOLDERS - definir valores reales en entorno seguro)
GMAIL_USER=<SET_IN_ENVIRONMENT>
GMAIL_APP_PASSWORD=<SET_IN_ENVIRONMENT>
COMPANY_EMAIL=<SET_IN_ENVIRONMENT>

# Recomendación: Añade también un correo destino distinto si necesitas auditoría
# CONTACT_RECIPIENT=<OPTIONAL_AUDIT_EMAIL>c

# (Fin)